services:
  - type: web
    name: nutriplan-backend
    env: node
    buildCommand: npm ci --omit=dev --legacy-peer-deps
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5001
    healthCheckPath: /api/health 